[project]
name = "autotrain-to-mlflow"
version = "0.1.0"
description = ""
authors = [
    {name = "Pedro José C.A",email = "pedrojos.campos@protonmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
